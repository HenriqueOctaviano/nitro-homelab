# 2026-08-21 Lab Journal

## Session Goal

Start documenting the Nitro homelab project in GitHub and make the repository the source of truth for the learning process.

## Initial Context

The target machine is an Acer Nitro 5 with:

- 16 GB RAM
- Two NVMe SSDs totaling approximately 1.5 TB
- Windows host

The intended lab scope includes:

- Kali Linux
- Linux and Windows servers
- APIs for other machines
- AI/JARVIS-style assistant experiments
- Infrastructure, networking, cybersecurity, DevOps and development practice

## Documentation Rule

Everything useful for reproducing the lab should be documented in GitHub:

- Commands
- Results
- Errors
- Fixes
- Decisions
- Architecture notes
- Learning notes

Sensitive information must not be committed.

## Information To Exclude

Do not commit:

- Passwords
- Tokens
- API keys
- SSH private keys
- Personal email addresses
- Product keys
- Unneeded serial numbers
- Home SSIDs
- Public IP addresses
- Personal file contents
- Private paths that reveal unnecessary personal information

## GitHub Access Decision

An earlier attempt expected the ChatGPT GitHub connection to create files directly in the repository. That path was not suitable for write operations in this workflow.

Result observed:

```text
403 Resource not accessible by integration
```

Decision:

- Use the local repository for hands-on Git learning.
- Use Codex/local tooling to edit and verify files.
- Use GitHub Desktop or Git commands for staging, committing and pushing as needed.

## Local Repository State

Checked the repository status.

Result:

```text
## main...origin/main
```

Observation:

The repository is clean and currently on `main`.

Branch `docs/project-foundation` exists for the initial documentation foundation.

Checked recent history.

Result:

```text
45176dd (HEAD -> main, origin/main, origin/docs/project-foundation, origin/HEAD) Initial commit
```

Observation:

The project currently has a single initial commit.

## Hardware Inventory

Hardware inventory and CPU-Z screenshots were collected.

Stable sanitized findings:

```text
Model: Acer Nitro 5 AN515-54
CPU: Intel Core i5-9300H, 4 cores / 8 threads
RAM: 16 GB DDR4-2666, dual channel
GPU: Intel UHD Graphics 630 + NVIDIA GeForce GTX 1650 4 GB
Storage: WDC PC SN520 ~477 GB + Kingston NV2 ~932 GB
OS: Windows 11 Pro
Secure Boot: Enabled
VirtualizationFirmwareEnabled: False
```

Observation:

The initial assumption of 32 GB RAM and 1 TB storage was incorrect. The real baseline is 16 GB RAM and approximately 1.5 TB across two NVMe SSDs.

No BIOS settings were changed.

## Storage Audit Started

A PowerShell storage audit was started on the Acer to generate:

```text
nitro-homelab-storage-audit/
├── c-drive-folders.csv
├── d-drive-folders.csv
└── files-over-1gb.csv
```

The goal is to classify data as:

```text
BACKUP
REVIEW
DISCARD
```

No files were deleted.

## First Documentation Step

Created the first documentation structure:

```text
docs/
  journal/
    2026-08-21.md
  00-preparation/
    README.md
    backup-plan.md
    storage-audit.md
  01-hardware/
    hardware-inventory.md
diagrams/
  README.md
```

Updated `README.md`, `ROADMAP.md` and `ARCHITECTURE.md` to describe the project foundation.

## Git CLI - Merge Foundation PR

The foundation pull request was merged into `main` on GitHub.

The local terminal initially could not find Git:

```text
git : The term 'git' is not recognized as the name of a cmdlet, function, script file, or operable program.
```

Resolution:

Used the Git executable bundled with GitHub Desktop.

Command:

```powershell
& "$env:LOCALAPPDATA\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe" --version
```

Result:

```text
git version 2.53.0.windows.4
```

Commands:

```powershell
& "$env:LOCALAPPDATA\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe" switch main
& "$env:LOCALAPPDATA\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe" pull
& "$env:LOCALAPPDATA\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe" branch -d docs/project-foundation
& "$env:LOCALAPPDATA\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe" status
```

Result:

```text
Switched to branch 'main'
Your branch is up to date with 'origin/main'.

Updating 45176dd..bbe1de5
Fast-forward
11 files changed, 732 insertions(+), 2 deletions(-)

Deleted branch docs/project-foundation (was 88ed3e9).

On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

Observation:

The merge was completed correctly, local `main` was updated, the old feature branch was removed locally and the working tree was clean.

## Storage Audit Analysis

The storage audit ZIP was reviewed without extracting raw CSV files into the repository.

ZIP contents:

```text
nitro-homelab-storage-audit/
├── c-drive-folders.csv
├── d-drive-folders.csv
└── files-over-1gb.csv
```

Sanitized summary:

```text
C: user profile area: 237.19 GB
C: Windows: 64.97 GB
C: Program Files: 50.80 GB
C: Program Files (x86): 25.81 GB
C: ProgramData: 16.80 GB

D: Steam library: 323.61 GB
D: World of Warcraft: 89.42 GB
D: Diablo IV: 86.46 GB
D: Riot Games: 33.20 GB
```

Large files summary:

```text
Files larger than 1 GB: 53
Total size: 302.57 GB
C: 160.36 GB
D: 142.21 GB
```

Initial category summary:

```text
Games and game data: 42 files, 181.19 GB
Needs review: 6 files, 93.94 GB
System files: 2 files, 12.59 GB
ISO files: 2 files, 8.60 GB
Personal archive needing review: 1 file, 6.25 GB
```

Decision:

Do not commit the raw CSV files because they may contain personal paths. Commit only sanitized summaries and backup decisions.
